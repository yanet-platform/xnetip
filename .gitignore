# Work plan and agent state are local to the maintainer's checkout.
.roadmap/
.claude/

# Build and test artifacts. rapid writes the failing case of a property
# test under testdata/rapid/; it is replayed, pinned as a unit case, and
# never committed.
bin/
testdata/rapid/
*.test
*.out
*.prof
